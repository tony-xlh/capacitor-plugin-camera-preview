import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dynamsoft.camerapreview',
  appName: 'camera-preview',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
